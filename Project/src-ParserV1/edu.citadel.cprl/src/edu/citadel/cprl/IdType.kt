package edu.citadel.cprl

/**
 * The types for identifiers stored in the identifier table.
 */
enum class IdType
  {
    constantId,
    variableId,
    arrayTypeId,
    recordTypeId,
    stringTypeId,
    fieldId,
    procedureId,
    functionId
  }
